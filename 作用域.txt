作用域：
当我们谈论到探索变量对就于代码的值的时候，作用域这个术语指的就是命名空间。也就是说，在代码中变量名被赋值的位置决定了这个变量能被访问的范围。

由于变量最初没有声明，Python将一个变量名被赋值的地点关联为（绑定给）一个特定的命名空间。换句话说，在代码中给一个变量赋值的地方决定了这个变量将存在于那个命名空间，也就是它的可见范围。

函数还为程序增加了一个额外的命名空间：在默认的情况下，一个函数的所有变量名都是与函数的命名空间相关联。